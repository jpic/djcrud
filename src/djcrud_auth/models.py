"""
djcrud_auth models.

This app provides authentication views and controllers but does NOT define
a User model. The User model should be defined in your project's app and
configured via settings.AUTH_USER_MODEL.

See djcrud_example/example_app/models.py for an example custom User model.
"""
